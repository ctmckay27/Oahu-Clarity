diff --git a/qwen_tts.c b/qwen_tts.c
index abbca11..d0dfb42 100644
--- a/qwen_tts.c
+++ b/qwen_tts.c
@@ -1059,7 +1059,10 @@ static void lookup_codec_embed(qwen_tts_ctx_t *ctx, int token_id, float *out) {
     qwen_bf16_to_f32_vec(out, emb, h);
 }
 
+#include "mari_native_controls.h"
+
 int qwen_tts_generate(qwen_tts_ctx_t *ctx, const char *text, float **out_samples, int *out_n_samples) {
+    if (mari_trajectory_init(ctx) != 0) return -1;
     qwen_exec_budget_engine_owned("cli");
     double t_start = time_ms();
     int h = ctx->config.hidden_size;
@@ -1109,6 +1112,10 @@ int qwen_tts_generate(qwen_tts_ctx_t *ctx, const char *text, float **out_samples
         return -1;
     }
 
+    if (mari_text_release_init(ctx,text_tokens,text_token_len)!=0) {
+        free(text_tokens);free(instruct_tokens);free(ref_text_tokens);return -1;
+    }
+
     int role_len = 3;
     int suffix_len = 5;
     int all_len = role_len + text_token_len + suffix_len;
@@ -1546,6 +1553,7 @@ int qwen_tts_generate(qwen_tts_ctx_t *ctx, const char *text, float **out_samples
     ctx->kv_len = delta_start;
 
     ctx->ml_steer_w_eff = 0.0f;
+    mari_trajectory_prefill(ctx);
 
     if (prefill_len > ctx->rope_cache_len) {
         fprintf(stderr, "Error: prompt too long (%d tokens > RoPE cache %d); shorten the text.\n",
@@ -1772,6 +1780,9 @@ int qwen_tts_generate(qwen_tts_ctx_t *ctx, const char *text, float **out_samples
             fprintf(stderr, "  [frame %d] EOS logit=%.2f rank=%d\n", frame, eos_logit, eos_rank);
         }
 
+        if (mari_release_frames && ctx->stream_trailing_pos < ctx->stream_trailing_len)
+            ctx->logits[QWEN_TTS_CODEC_EOS] = -1e30f;
+
         float frame_temp = ctx->temperature;
         int frame_top_k = ctx->top_k;
         if (ctx->greedy_warmup > 0 && frame < ctx->greedy_warmup) {
@@ -1847,10 +1858,12 @@ int qwen_tts_generate(qwen_tts_ctx_t *ctx, const char *text, float **out_samples
             }
         }
 
-        if (ctx->stream_layout_active && frame < ctx->stream_trailing_len) {
-            const float *tail = ctx->stream_trailing_text + (size_t)frame * h;
+        int text_pos = mari_release_frames ? ctx->stream_trailing_pos : frame;
+        if (ctx->stream_layout_active && text_pos < ctx->stream_trailing_len && mari_text_ready(text_pos+1,frame)) {
+            const float *tail = ctx->stream_trailing_text + (size_t)text_pos * h;
             for (int j = 0; j < h; j++) step_embed[j] += tail[j];
-            ctx->stream_trailing_pos = frame + 1;
+            ctx->stream_trailing_pos = text_pos + 1;
+            mari_text_record(text_pos+1,frame);
         } else {
             for (int j = 0; j < h; j++) step_embed[j] += tts_pad_embed[j];
         }
@@ -1870,6 +1883,7 @@ int qwen_tts_generate(qwen_tts_ctx_t *ctx, const char *text, float **out_samples
                 ctx->ml_steer_w_eff = ctx->ml_steer_weight * powf(g, (float)frame);
             }
         }
+        mari_trajectory_step(ctx, frame);
         double t_step_start = time_ms();
         if (qwen_talker_step(ctx, step_embed, last_hidden) != 0) {
             free(step_embed); free(last_hidden);
diff --git a/qwen_tts_talker.c b/qwen_tts_talker.c
index 3b49880..91bf8d0 100644
--- a/qwen_tts_talker.c
+++ b/qwen_tts_talker.c
@@ -18,6 +18,7 @@
 #define QWEN_HAVE_MADVISE 1
 #endif
 
+static FILE *g_mari_seq = NULL;
 static const char *g_actmap_path   = NULL;
 static double    **g_actmap_acc    = NULL;
 static int         g_actmap_layers = 0;
@@ -26,6 +27,7 @@ static long        g_actmap_frames = 0;
 static int         g_actmap_probed = 0;
 
 static void actmap_dump(void) {
+    if(g_mari_seq){int failed=fflush(g_mari_seq)!=0 || ferror(g_mari_seq); if(fclose(g_mari_seq)!=0)failed=1; g_mari_seq=NULL; if(failed){perror("MARI capture flush");_Exit(72);}}
     if (!g_actmap_path || g_actmap_frames <= 0 || !g_actmap_acc) return;
     FILE *f = fopen(g_actmap_path, "wb");
     if (!f) return;
@@ -53,11 +55,19 @@ static void actmap_init(int num_layers, int h) {
     g_actmap_acc    = (double **)calloc(g_actmap_layers, sizeof(double *));
     if (!g_actmap_acc) { g_actmap_path = NULL; return; }
     for (int l = 0; l < g_actmap_layers; l++) g_actmap_acc[l] = (double *)calloc(h, sizeof(double));
+    const char *seq=getenv("MARI_ACT_SEQUENCE");
+    if(seq && *seq) {
+        g_mari_seq=fopen(seq,"wb");
+        if(!g_mari_seq){perror("MARI_ACT_SEQUENCE");exit(72);}
+        uint32_t header[3]={0x3151534d,(uint32_t)g_actmap_layers,(uint32_t)g_actmap_dim};
+        if(fwrite(header,4,3,g_mari_seq)!=3)exit(72);
+    }
     atexit(actmap_dump);
 }
 
 static inline void actmap_accum(int layer, const float *x, int h) {
     if (!g_actmap_path || !g_actmap_acc) return;
+    if(g_mari_seq && fwrite(x,sizeof(float),h,g_mari_seq)!=(size_t)h)exit(72);
     double *a = g_actmap_acc[layer];
     for (int i = 0; i < h; i++) a[i] += (double)x[i];
 }
@@ -1783,6 +1793,19 @@ int qwen_talker_prefill(qwen_tts_ctx_t *ctx, float *input_embeds, int seq_len) {
 
         qwen_region_end(QWEN_RGN_TK_PF_DOWN);
 
+        /* Explicit numeric onset conditioning affects only the final prompt
+         * position that predicts the first codec frame, never the text prefix. */
+        if (ctx->ml_steer && ctx->ml_steer_w_eff != 0.0f &&
+            layer >= ctx->ml_steer_l0 && layer <= ctx->ml_steer_l1) {
+            float *x = residual + (int64_t)(n_new-1)*h;
+            const float *v = ctx->ml_steer + (size_t)layer*ctx->ml_steer_dim;
+            float n0=0.0f,n1=0.0f;
+            for(int i=0;i<h;i++)n0+=x[i]*x[i];
+            for(int i=0;i<h;i++)x[i]+=ctx->ml_steer_w_eff*v[i];
+            for(int i=0;i<h;i++)n1+=x[i]*x[i];
+            if(n1>1e-12f){float scale=sqrtf(n0/n1);for(int i=0;i<h;i++)x[i]*=scale;}
+        }
+
         if (ctx->debug) {
             fprintf(stderr, "  Layer %d/%d done", layer + 1, c->num_layers);
             fprintf(stderr, " res[:4]=[%.4f,%.4f,%.4f,%.4f]",
